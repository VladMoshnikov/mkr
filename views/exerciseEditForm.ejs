<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Редагувати результат</title>
    <link rel="stylesheet" href="/css/styles.css">
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&display=swap" rel="stylesheet">
</head>
<body>
    <div class="container">
        <header>
            <h1>Редагувати результат</h1>
        </header>
        <form action="/exercises/edit/<%= exercise.id %>" method="POST">
            <label for="pib">ПІБ:</label>
            <input type="text" id="pib" name="pib" value="<%= exercise.pib %>" required>
            <label for="run100m">100 метрів (формат HH:MM:SS):</label>
            <input type="time" id="run100m" name="run100m" value="<%= exercise.run100m %>" step="1" required>
            <label for="run3km">3 км (формат HH:MM:SS):</label>
            <input type="time" id="run3km" name="run3km" value="<%= exercise.run3km %>" step="1" required>
            <label for="pushups">Віджимання:</label>
            <input type="number" id="pushups" name="pushups" value="<%= exercise.pushups %>" required>
            <label for="gender">Стать:</label>
            <select id="gender" name="gender" required>
                <option value="male" <%= exercise.gender === 'male' ? 'selected' : '' %>>Чоловік</option>
                <option value="female" <%= exercise.gender === 'female' ? 'selected' : '' %>>Жінка</option>
            </select>
            <button type="submit">Оновити</button>
        </form>
    </div>
</body>
</html>
